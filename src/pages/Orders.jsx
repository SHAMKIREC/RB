import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { MapPin, MessageCircle } from 'lucide-react';
import { getPublishedOrders } from '../lib/ordersStorage';
import OrderResponseModal from '../components/OrderResponseModal';
const money = (value) => `${Math.round(Number(value) || 0).toLocaleString('ru-RU')} ₽`;
const payment = (order) => Number(order.contractorPayment ?? order.finalTotal ?? order.total ?? order.calculatedTotal ?? 0) || 0;
export default function Orders() { const [orders, setOrders] = useState([]); const [responseOrder, setResponseOrder] = useState(null); useEffect(() => { getPublishedOrders().then(setOrders).catch(() => setOrders([])); }, []); return <div className="page-shell py-7 sm:py-10"><div className="mb-5"><p className="mb-1 text-xs font-mono font-bold uppercase tracking-widest text-primary">Заказы</p><h1 className="mb-2 text-3xl font-black text-foreground sm:text-4xl">Активные заказы</h1><p className="max-w-2xl text-sm text-muted-foreground">Выберите подходящий заказ и свяжитесь с нами, указав номер сметы.</p></div>{orders.length ? <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{orders.map((order) => <article key={order.id} className="rb-card rb-card-action overflow-hidden rounded-2xl">{order.photos?.[0] && <img src={order.photos[0]} alt={order.title} className="aspect-[16/9] w-full object-cover" />}<div className="p-5"><div className="flex justify-between"><span className="font-mono text-xs font-bold text-primary">Заказ №{order.number}</span><span className="rounded-full bg-green-500/10 px-2 py-1 text-[10px] font-bold text-green-700">Активен</span></div><h2 className="mt-2 text-lg font-black">{order.title}</h2><p className="mt-1 flex gap-1 text-sm text-muted-foreground"><MapPin className="h-3.5 w-3.5" />{order.location}</p><p className="mt-3 line-clamp-2 text-sm text-muted-foreground">{order.description}</p><div className="mt-4 grid grid-cols-2 gap-2 text-xs"><span>{order.selectedWorks?.length || 0} видов работ</span><b className="text-right text-primary">Оплата исполнителю: {money(payment(order))}</b><span>Срок: {order.preferredDeadline || 'по договорённости'}</span></div><div className="mt-5 grid grid-cols-2 gap-2"><Link to={`/orders/${order.id}`} className="rounded-xl border border-primary/30 py-2.5 text-center text-xs font-bold hover:bg-primary/5">Посмотреть смету</Link><button onClick={() => setResponseOrder(order)} className="rounded-xl bg-primary py-2.5 text-xs font-bold text-white"><MessageCircle className="mr-1 inline h-4 w-4" />Откликнуться</button></div></div></article>)}</div> : <div className="rounded-2xl border border-dashed border-border px-6 py-14 text-center text-muted-foreground">Активных заказов пока нет.</div>}{responseOrder && <OrderResponseModal number={responseOrder.number} onClose={() => setResponseOrder(null)} />}</div>; }
